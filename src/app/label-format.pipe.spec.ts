import { LabelFormatPipe } from './label-format.pipe';

describe('LabelFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
